#include <stdio.h>

int main(){

    // Area calculator using hard coded values
    int length = 10;
    int breadth = 20;

    int area = length * breadth;

    printf("Area of a rectange of length %d and breadth %d is %d \n\n",length ,breadth, area );

    return 0;
}